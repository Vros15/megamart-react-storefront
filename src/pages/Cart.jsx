/**
 * Cart items, totals, and checkout land here in tasks 6-8. Placeholder content
 * only, so routing can be verified before cart state exists.
 */
const Cart = () => (
  <>
    <h1>Your Cart</h1>
    <p>Cart contents placeholder.</p>
  </>
);

export default Cart;
